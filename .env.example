# Подключение к PostgreSQL для Prisma.
# Заполнить на фазе подключения БД (скопировать в .env).
DATABASE_URL="postgresql://user:password@localhost:5432/stockpoisk?schema=public"

# Токен админ-панели (заголовок x-admin-token). В dev = dev; в проде задать секрет.
# ВНИМАНИЕ: фронт пока шлёт 'dev' (frontend/src/api/admin.ts) — это временно (см. STATUS).
ADMIN_TOKEN="dev"

# VK ID OAuth («Войти через ВК» на сайте) + подпись launch-параметров Mini App.
# Взять в dev.vk.com → настройки приложения. VK_APP_SECRET — «Защищённый ключ».
VK_APP_ID=""
VK_APP_SECRET=""

# Секрет подписи сессий (httpOnly-cookie). Сгенерировать: openssl rand -hex 32
SESSION_SECRET=""

# ВРЕМЕННО: тестовый вход по имени (пока VK ID не подключён). ВКЛЮЧЁН по умолчанию.
# Выключить при запуске VK ID: ALLOW_TEST_LOGIN=0
ALLOW_TEST_LOGIN=""
